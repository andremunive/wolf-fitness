[build]
  command = "npm run build"
  publish = "dist/wolf-fitness-gym"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback: todas las rutas no matcheadas devuelven index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
